/* import { Controller, Post, UploadedFile, UseInterceptors } from '@nestjs/common';
import { FileInterceptor } from '@nestjs/platform-express';

@Controller('upload')
export class AppController {
  @Post('pdf')
  @UseInterceptors(FileInterceptor('file'))
  async uploadPdf(@UploadedFile() file) {
    if (!file) {
      return { message: 'No se proporcionó ningún archivo PDF' };
    }

    // Puedes realizar cualquier otra lógica aquí, como guardar en la base de datos

    return {
      message: 'Archivo PDF cargado y guardado correctamente',
      originalname: file.originalname,
      destination: file.destination,
      filename: file.filename,
    };
  }
}
 */